@model IEnumerable<_5_Stars_Reviews.Models.ActorComment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Actor.ActorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AComment)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Actor.ActorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AComment)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ACommentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ACommentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ACommentId })
        </td>
    </tr>
}

</table>
